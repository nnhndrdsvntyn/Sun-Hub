local function displayMessage(message)
    local Players = game:GetService("Players")
    local StarterGui = game:GetService("StarterGui")
    local LocalPlayer = Players.LocalPlayer
    
    if LocalPlayer then
        StarterGui:SetCore("ChatMakeSystemMessage", {
            Text = "[Sun Hub]: " .. message;
            Color = Color3.fromRGB(0, 255, 0);
            Font = Enum.Font.SourceSansBold;
            TextSize = 18;
        })
    end
end

displayMessage("Loaded Sun Hub, For TSB.")

local function displaySubtitleMessage(message)
    local Players = game:GetService("Players")
    local StarterGui = game:GetService("StarterGui")
    local LocalPlayer = Players.LocalPlayer
    
    if LocalPlayer then
        StarterGui:SetCore("ChatMakeSystemMessage", {
            Text = "" .. message;
            Color = Color3.fromRGB(0, 255, 0);
            Font = Enum.Font.SourceSansBold;
            TextSize = 15;
        })
    end
end

local Version = "BETA"
displaySubtitleMessage("Version: " .. Version)

local StarterGui = game:GetService("StarterGui")
StarterGui:SetCore("SendNotification", {
    Title = "Sun Hub",
    Text = "Game: The Strongest Battle Grounds\nMade By NNHN\nLoading... ✅", 
    Button1 = "OK.",
    Duration = 9999999999999999999999999999999999999999999999999999999999999, -- duration of the notification in seconds
})
